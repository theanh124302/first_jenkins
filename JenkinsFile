pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Bước này để checkout mã nguồn từ GitHub
                checkout scm
            }
        }
    }
}